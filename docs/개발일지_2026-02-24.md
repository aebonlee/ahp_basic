# 개발일지 2026-02-24

## 주요 변경 사항

### 1. [Critical] 평가 데이터 저장 불가 버그 수정 — evaluator_id FK 불일치

**문제**: `pairwise_comparisons.evaluator_id`는 `evaluators.id` (평가자 레코드 UUID)를 참조하는 FK인데,
코드에서 `user?.id` (auth.users.id, 인증 UUID)를 전달하고 있었음.
결과적으로 모든 쌍대비교 저장이 FK 제약조건 위반으로 **무조건 실패**하여 평가 데이터가 DB에 저장되지 않았음.

**원인 분석**:
- `evaluators` 테이블: `id` (PK, 자체 UUID), `user_id` (auth.users.id 참조)
- `pairwise_comparisons` 테이블: `evaluator_id` → `evaluators.id` 참조 (FK)
- 코드: `evaluatorId={user?.id}` → auth UUID를 evaluators.id로 잘못 전달

**수정 내용** (4개 페이지 + Context):
| 파일 | 변경 |
|------|------|
| `PairwiseRatingPage.jsx` | `useEvaluators(id)` 추가, `evaluators.find(e => e.user_id === user?.id)?.id`로 올바른 ID 조회 |
| `DirectInputPage.jsx` | 동일 패턴 적용 |
| `EvalResultPage.jsx` | 동일 패턴 적용 |
| `SignaturePanel.jsx` | `.eq('user_id', evaluatorId)` → `.eq('id', evaluatorId)` 수정 |
| `EvaluationContext.jsx` | `loadProjectData(projectId, evaluatorId)` 시그니처 변경, evaluator별 비교데이터 필터링 추가 |

---

### 2. 평가 진행도 표시 개선

**문제**: 사용자가 "0/12 진행도 표시도 되게 해줘"라고 요청. 기존 진행바는 페이지 진행률만 표시하고 있었고,
evaluator_id 버그로 인해 완료 항목이 항상 0으로 표시됨.

**수정 내용**:
- `EvaluationProgress.jsx`: 진행바가 실제 셀 완료율을 반영하도록 변경
- 표시 형식: "평가 완료: **3/12** 항목 (25%)"
- 스타일 개선: 배경 카드, 더 두꺼운 진행바 (8→10px), 그라디언트 색상

---

### 3. 대시보드 폰트 크기 전면 조정

**문제**: "대시보드 디자인에 영역에 비해 폰트들의 크기가 작아서 알아보기 어려워"

**수정 내용**:

| 요소 | 이전 | 이후 |
|------|------|------|
| 페이지 타이틀 | 1.4rem | 1.6rem |
| 프로젝트명 | 1.15rem | 1.35rem |
| 상태 배지 | 0.72rem | 0.85rem |
| 프로젝트 설명 | 0.85rem | 1.0rem |
| 단계명 | 0.88rem | 1.05rem |
| 단계 설명 | 0.75rem | 0.9rem |
| 단계 원형번호 | 28px/0.75rem | 34px/0.9rem |
| 섹션 제목 (프로젝트) | 1.0rem | 1.2rem |
| 필터 버튼 | 0.75rem | 0.9rem |
| 카드 이름 | 0.9rem | 1.05rem |
| 카드 배지 | 0.7rem | 0.82rem |
| 카드 설명 | 0.8rem | 0.95rem |
| 액션 버튼 | 0.75rem | 0.88rem |

---

## 수정 파일 목록

```
src/pages/PairwiseRatingPage.jsx        — evaluatorId 수정
src/pages/DirectInputPage.jsx           — evaluatorId 수정
src/pages/EvalResultPage.jsx            — evaluatorId 수정
src/components/results/SignaturePanel.jsx — evaluator 쿼리 수정
src/contexts/EvaluationContext.jsx       — loadProjectData evaluator 필터링
src/components/evaluation/EvaluationProgress.jsx      — 진행도 표시 개선
src/components/evaluation/EvaluationProgress.module.css — 진행도 스타일
src/pages/AdminDashboard.module.css     — 대시보드 폰트 확대
src/components/admin/ProjectPanel.module.css — 프로젝트 패널 폰트 확대
src/components/admin/ProjectCard.module.css  — 프로젝트 카드 폰트 확대
```

---

### 4. "I Make It" / "imakeit" 브랜딩 완전 제거

**요청**: "소스에서 그리고 보여지는 비주얼부분에서도 i make it 이란 단어 보이지 않게 해줘. 다른 회사야."

**수정 내용** (5개 파일):

| 파일 | 이전 | 이후 |
|------|------|------|
| `index.html` | `<title>AHP Basic - I Make It</title>` | `<title>AHP Basic - 의사결정 분석 도구</title>` |
| `index.html` | `og:title="AHP Basic - I Make It"` | `og:title="AHP Basic - 의사결정 분석 도구"` |
| `Navbar.jsx` | 로고 서브텍스트 "I Make It" | "Decision Analysis" |
| `Footer.jsx` | "AHP Basic - I Make It" + imakeit.kr 링크 | "AHP Basic" + ahp-basic.dreamitbiz.com |
| `HomePage.jsx` | "AHP Basic - I Make It" 푸터 | "AHP Basic" |
| `helpData.js` | "원본 imakeit.kr" 주석 | 주석 제거 |

---

### 5. 평가자 페이지 디자인 전면 개편

**요청**: "평가하기 디자인도 더 개선해줘. 독특하게 해줘."

#### EvaluatorMainPage 개편:
- **배너**: 네이비→블루 그라디언트 (linear-gradient 135deg, #1e3a5f→#2563eb)
- **프로젝트 카드**: 단일 리스트 → 카드 그리드 (auto-fill, minmax(320px, 1fr))
- **섹션 분리**: "진행 중인 평가" / "완료된 평가" 로 분리, 컬러 도트 + 카운트 배지
- **카드 디테일**: 상태칩(색상별) + 평가방법 배지, 제목 + 설명(2줄 clamp), 호버 그림자
- **빈 상태**: SVG 일러스트레이션 + 안내 문구
- **완료 카드**: 반투명(opacity 0.75), 호버 시 복원

#### AhpIntroduction 개편:
- **레이아웃**: 단일 박스 → 히어로(SVG 아이콘 + 제목) + 3단 스텝카드
- **스텝카드**: 번호 원형(그라디언트 보라) + 제목 + 설명, 카드형 배치
- **척도표**: `<details>`/`<summary>` 접이식으로 변경 (기본 접힘)
- **타이포**: 제목 1.3→1.5rem, 본문 0.85→0.95rem, 카드제목 1.05rem

---

## 수정 파일 목록 (전체)

```
# 커밋 1: evaluator_id FK 수정 + 대시보드 폰트
src/pages/PairwiseRatingPage.jsx        — evaluatorId 수정
src/pages/DirectInputPage.jsx           — evaluatorId 수정
src/pages/EvalResultPage.jsx            — evaluatorId 수정
src/components/results/SignaturePanel.jsx — evaluator 쿼리 수정
src/contexts/EvaluationContext.jsx       — loadProjectData evaluator 필터링
src/components/evaluation/EvaluationProgress.jsx      — 진행도 표시 개선
src/components/evaluation/EvaluationProgress.module.css — 진행도 스타일
src/pages/AdminDashboard.module.css     — 대시보드 폰트 확대
src/components/admin/ProjectPanel.module.css — 프로젝트 패널 폰트 확대
src/components/admin/ProjectCard.module.css  — 프로젝트 카드 폰트 확대

# 커밋 2: 브랜딩 제거 + 평가자 디자인
index.html                              — 타이틀/OG태그 변경
src/components/layout/Navbar.jsx        — 로고 서브텍스트 변경
src/components/layout/Footer.jsx        — 저작권/링크 변경
src/pages/HomePage.jsx                  — 푸터 변경
src/lib/helpData.js                     — 주석 제거
src/pages/EvaluatorMainPage.jsx         — 전면 재설계
src/pages/EvaluatorMainPage.module.css  — 전면 재설계
src/components/evaluation/AhpIntroduction.jsx      — 전면 재설계
src/components/evaluation/AhpIntroduction.module.css — 전면 재설계
```

---

### 6. 평가 진행률 계산 버그 수정 + 컬러 체계 전면 개선

**문제**: "평가 결과에도 평가 진행: 9/12 진행율이 달라. 다시 점검하고, 컬러 사용에 대해서도 조정해줘"

#### 진행률 계산 버그

**근본 원인**: 완료 판정 조건 `comparisons[key] !== 0`이 이전 버그(`getCellValue(8)=0`)로 저장된 "동등" 데이터를 미완료로 처리.
예: 12개 비교 중 3개가 "동등"(DB에 0으로 저장) → 9/12로 표시 (실제는 12/12 완료)

**수정 내용**:
| 파일 | 이전 | 이후 |
|------|------|------|
| `EvalResultPage.jsx` | `comparisons[key] !== undefined && comparisons[key] !== 0` | `comparisons[key] !== undefined` (존재 여부만 확인) |
| `EvaluationProgress.jsx` | 동일 | 동일 |
| `PairwiseRow.jsx` | `comparisons[compKey] \|\| 0` (값 0이면 미선택 처리) | `rawValue === 0 ? 1 : rawValue` (DB의 0을 1로 변환) |
| `PairwiseRow.jsx` | `currentValue === 0 → return -1` (미선택) | `currentValue === null → return -1` (null만 미선택) |
| `PairwiseRow.jsx` | evaluatorId null이어도 저장 시도 | `if (!evaluatorId) return;` 가드 추가 |

#### SignaturePanel 개선
- 텍스트만 표시 → **진행바 + 퍼센트** 표시 (오렌지→그린 그라디언트)
- 100% 완료 시 초록색 진행바 자동 전환
- 비일관성 경고 별도 표시
- 완료 시 체크 아이콘 + 성공 토스트

#### 컬러 체계 전면 개선 (variables.css)

| 영역 | 이전 (3자리 축약) | 이후 (Tailwind 호환) |
|------|-----------------|---------------------|
| 쌍대비교 왼쪽 | `#38d` | `#3b82f6` (blue-500) |
| 쌍대비교 오른쪽 | `#e77` | `#ef4444` (red-500) |
| 선택 배경 | `rgba(30,250,200,0.3)` (민트) | `rgba(59,130,246,0.2)` (파란 계열로 통일) |
| 선택 보더 | `#009` | `#1d4ed8` (blue-700) |
| 우선순위 바 | `#3a2` (탁한 녹색) | `#2563eb` (blue-600, 그라디언트) |
| 레벨 1-5 | 3자리 축약 | 풀 6자리 Tailwind 컬러 |
| 성공 | `#393` | `#16a34a` (green-600) |
| 위험 | `#e33` | `#dc2626` (red-600) |
| 상태: 생성중 | `#999` | `#94a3b8` (slate-400) |
| 상태: 평가중 | `#0a0` | `#22c55e` (green-500) |

#### 결과 페이지 폰트 확대

| 요소 | 이전 | 이후 |
|------|------|------|
| 결과 페이지 제목 | 1.3rem | 1.5rem |
| 탭 버튼 | 0.9rem | 1.05rem |
| 결과 테이블 | 0.85rem | 0.95rem |
| 테이블 헤더 | 0.8rem | 0.9rem (배경 추가) |
| 바 라벨 | 0.8rem | 0.95rem |
| 바 높이 | 14px | 18px |
| 블록 제목 | 0.9rem | 1.05rem |

---

## 수정 파일 목록 (전체)

```
# 커밋 1: evaluator_id FK 수정 + 대시보드 폰트
src/pages/PairwiseRatingPage.jsx
src/pages/DirectInputPage.jsx
src/pages/EvalResultPage.jsx
src/components/results/SignaturePanel.jsx
src/contexts/EvaluationContext.jsx
src/components/evaluation/EvaluationProgress.jsx
src/components/evaluation/EvaluationProgress.module.css
src/pages/AdminDashboard.module.css
src/components/admin/ProjectPanel.module.css
src/components/admin/ProjectCard.module.css

# 커밋 2: 브랜딩 제거 + 평가자 디자인
index.html
src/components/layout/Navbar.jsx
src/components/layout/Footer.jsx
src/pages/HomePage.jsx
src/lib/helpData.js
src/pages/EvaluatorMainPage.jsx
src/pages/EvaluatorMainPage.module.css
src/components/evaluation/AhpIntroduction.jsx
src/components/evaluation/AhpIntroduction.module.css

# 커밋 3: 진행률 수정 + 컬러 개선
src/pages/EvalResultPage.jsx             — 완료 판정 로직 수정
src/pages/EvalResultPage.module.css      — 제목/탭 폰트 확대
src/components/evaluation/EvaluationProgress.jsx — 완료 판정 수정
src/components/evaluation/PairwiseRow.jsx — value=0 처리 + evaluatorId 가드
src/components/results/SignaturePanel.jsx — 진행바 + 퍼센트 UI 개편
src/styles/results.module.css            — SignaturePanel + 결과 폰트 확대
src/styles/variables.css                 — 전체 컬러 팔레트 교체
src/styles/pairwise.module.css           — 행 배경색 조정
src/components/evaluation/ConsistencyDisplay.module.css — 경고 펄스 애니메이션
src/components/evaluation/PriorityBarChart.module.css   — 바 그라디언트 + 높이
```

### 7. 미완료/비일관성 항목 재점검 + 페이지 네비게이터 상태 표시

**커밋**: `da91ec2` — `feat: 미완료/비일관성 항목 재점검 + 페이지 네비게이터 상태 표시`

**문제**: 평가 결과 페이지에서 미완료/비일관성 초과 항목이 있어도 어떤 항목이 문제인지 확인할 수 없었고,
페이지 네비게이터에서 각 페이지의 완료 상태를 시각적으로 파악할 수 없었음.

**수정 내용**:
- **결과 페이지(EvalResultPage)**: 미완료 항목 및 비일관성 초과 항목 목록을 표시하고 "돌아가기" 버튼으로 해당 페이지로 직접 이동 가능
- **페이지 네비게이터(PageNavigator)**: 도트 색상으로 완료 상태 표시 (초록=완료, 주황=일부, 회색=미시작, 파랑=현재)
- **해시 네비게이션**: `#page=N` 해시로 특정 페이지 직접 이동 지원
- **PairwiseRatingPage**: 해시 기반 페이지 이동 로직 추가

| 파일 | 변경 |
|------|------|
| `src/pages/EvalResultPage.jsx` | 미완료/비일관성 항목 목록 + 돌아가기 버튼 |
| `src/pages/EvalResultPage.module.css` | 재점검 영역 스타일 |
| `src/pages/PairwiseRatingPage.jsx` | #page=N 해시 네비게이션 처리 |
| `src/components/evaluation/PageNavigator.jsx` | 도트 색상 상태 표시 로직 |
| `src/components/evaluation/PageNavigator.module.css` | 상태별 도트 색상 스타일 |

---

### 8. 사이트 전체 기본 폰트 14px → 16px 확대

**커밋**: `dbad9d0` — `style: 사이트 전체 기본 폰트 14px → 16px 확대`

**문제**: 사이트 전체적으로 폰트가 작아 가독성이 떨어짐.

**수정 내용**:
- `src/index.css`에서 `html` 기본 `font-size`를 `14px` → `16px`로 변경
- 모든 `rem` 단위가 이 기본값에 연동되므로 사이트 전체 폰트 크기가 일괄 확대됨

| 파일 | 변경 |
|------|------|
| `src/index.css` | `font-size: 14px` → `font-size: 16px` |

---

### 9. [Critical] 상위 기준 비교 저장 실패 수정 (parentId='root' → project UUID)

**커밋**: `b77c2a6` — `fix: 상위 기준 비교 저장 실패 수정 (parentId='root' → project UUID)`

**문제**: `buildPageSequence`에서 최상위 기준의 `parentId`를 문자열 `'root'`로 설정하고 있었음.
DB의 `criterion_id` 컬럼은 UUID 타입이므로 `'root'` 문자열 저장 시 타입 오류로 데이터가 무시됨.
결과적으로 **최상위 기준 간의 쌍대비교 데이터가 저장되지 않는** 심각한 버그.

**수정 내용**:
- 프로젝트 ID(UUID)를 `goalId`로 사용하여 최상위 기준의 `parentId`를 유효한 UUID로 설정
- 11개 파일에서 `buildPageSequence` 호출부 및 관련 로직 일관 수정

| 파일 | 변경 |
|------|------|
| `src/lib/pairwiseUtils.js` | `parentId: 'root'` → `parentId: goalId` (프로젝트 UUID) |
| `src/pages/PairwiseRatingPage.jsx` | goalId 전달 |
| `src/pages/DirectInputPage.jsx` | goalId 전달 |
| `src/pages/EvalResultPage.jsx` | goalId 전달 |
| `src/pages/AdminResultPage.jsx` | goalId 전달 + 집계 로직 수정 |
| `src/pages/ResourceAllocationPage.jsx` | goalId 전달 |
| `src/pages/SensitivityPage.jsx` | goalId 전달 |
| `src/pages/WorkshopPage.jsx` | goalId 전달 |
| `src/components/results/ResultSummary.jsx` | goalId 전달 |
| `src/components/results/ComprehensiveChart.jsx` | goalId 전달 |
| `src/lib/exportUtils.js` | goalId 전달 |

---

### 10. 전체 디자인 시스템 전면 교체 (Warm Violet 아이덴티티) + AdminResultPage 집계 수정

**커밋**: `00573fe` — `feat: 전체 디자인 시스템 전면 교체 (Warm Violet 아이덴티티)`

**문제**: 기존 블루 계열 디자인이 일관성 부족하고, AdminResultPage에서 value=0인 비교데이터가 집계에서 누락됨.

**수정 내용**:

#### 디자인 시스템 교체
- **컬러 팔레트**: 기존 블루(`#0066CC`) → 바이올렛(`#7c3aed`) + 앰버 악센트
- **Navbar/Footer**: 다크 슬레이트 배경, 앰버 모드 스위치
- **HomePage**: 다크 히어로 + 그라데이션 타이틀, 카드 호버 효과
- **버튼**: 라운드 pill 스타일, 바이올렛 그림자
- **카드**: 따뜻한 보더, 호버 시 상단 악센트 라인
- **평가 UI**: 바이올렛-오렌지 쌍대비교 색상, 모노스페이스 수치
- **공통**: 따뜻한 배경(`#f7f5f3`), 개선된 타이포그래피, 커스텀 스크롤바

#### AdminResultPage 집계 수정
- `value === 0`인 비교 데이터가 집계에서 누락되던 버그 수정

| 파일 | 변경 |
|------|------|
| `src/styles/variables.css` | 전체 컬러 팔레트 교체 (바이올렛 + 앰버) |
| `src/index.css` | 글로벌 스타일 (배경, 스크롤바 등) |
| `src/components/layout/Navbar.module.css` | 다크 슬레이트 배경, 앰버 악센트 |
| `src/components/layout/Footer.module.css` | 다크 슬레이트 배경 |
| `src/components/layout/PageLayout.module.css` | 따뜻한 배경 적용 |
| `src/pages/HomePage.jsx` | 다크 히어로 섹션 |
| `src/pages/HomePage.module.css` | 히어로 그라데이션 + 카드 호버 |
| `src/components/common/Button.module.css` | pill 스타일 + 바이올렛 그림자 |
| `src/styles/common.module.css` | 카드 보더 + 악센트 라인 |
| `src/pages/AdminDashboard.module.css` | 대시보드 Warm Violet 적용 |
| `src/components/admin/ProjectPanel.module.css` | 패널 스타일 업데이트 |
| `src/components/admin/ProjectCard.module.css` | 카드 스타일 업데이트 |
| `src/pages/EvaluatorMainPage.module.css` | 평가자 메인 스타일 |
| `src/components/evaluation/AhpIntroduction.module.css` | AHP 소개 스타일 |
| `src/components/evaluation/PageNavigator.module.css` | 네비게이터 스타일 |
| `src/components/evaluation/EvaluationProgress.module.css` | 진행바 스타일 |
| `src/components/evaluation/BestFitHelper.module.css` | 헬퍼 스타일 |
| `src/components/evaluation/ConsistencyDisplay.module.css` | 일관성 표시 스타일 |
| `src/components/evaluation/PriorityBarChart.module.css` | 우선순위 차트 스타일 |
| `src/pages/PairwiseRatingPage.module.css` | 쌍대비교 스타일 |
| `src/pages/EvalResultPage.module.css` | 결과 페이지 스타일 |
| `src/styles/pairwise.module.css` | 쌍대비교 공통 스타일 |
| `src/styles/results.module.css` | 결과 공통 스타일 |
| `src/pages/AdminResultPage.jsx` | value=0 집계 누락 수정 |

---

## 수정 파일 목록 (커밋 4~7)

```
# 커밋 4: 미완료/비일관성 재점검 + 페이지 네비게이터
src/pages/EvalResultPage.jsx
src/pages/EvalResultPage.module.css
src/pages/PairwiseRatingPage.jsx
src/components/evaluation/PageNavigator.jsx
src/components/evaluation/PageNavigator.module.css

# 커밋 5: 사이트 전체 기본 폰트 확대
src/index.css

# 커밋 6: 상위 기준 비교 저장 실패 수정
src/lib/pairwiseUtils.js
src/pages/PairwiseRatingPage.jsx
src/pages/DirectInputPage.jsx
src/pages/EvalResultPage.jsx
src/pages/AdminResultPage.jsx
src/pages/ResourceAllocationPage.jsx
src/pages/SensitivityPage.jsx
src/pages/WorkshopPage.jsx
src/components/results/ResultSummary.jsx
src/components/results/ComprehensiveChart.jsx
src/lib/exportUtils.js

# 커밋 7: 전체 디자인 시스템 교체 (Warm Violet) + AdminResultPage 수정
src/styles/variables.css
src/index.css
src/components/layout/Navbar.module.css
src/components/layout/Footer.module.css
src/components/layout/PageLayout.module.css
src/pages/HomePage.jsx
src/pages/HomePage.module.css
src/components/common/Button.module.css
src/styles/common.module.css
src/pages/AdminDashboard.module.css
src/components/admin/ProjectPanel.module.css
src/components/admin/ProjectCard.module.css
src/pages/EvaluatorMainPage.module.css
src/components/evaluation/AhpIntroduction.module.css
src/components/evaluation/PageNavigator.module.css
src/components/evaluation/EvaluationProgress.module.css
src/components/evaluation/BestFitHelper.module.css
src/components/evaluation/ConsistencyDisplay.module.css
src/components/evaluation/PriorityBarChart.module.css
src/pages/PairwiseRatingPage.module.css
src/pages/EvalResultPage.module.css
src/styles/pairwise.module.css
src/styles/results.module.css
src/pages/AdminResultPage.jsx
```

### 11. 컬러 시스템 변경 (Warm Violet → Dark Blue) + 대시보드 폰트 확대 + 연구자/평가자 가이드 추가

**요청**: 대시보드에 연구자 가이드 패널 추가, 전체 컬러를 다크블루로 교체, 대시보드 폰트 크기/행간 개선, 평가자 완전 가이드 추가

#### 11-1. 컬러 시스템 변경 (Warm Violet → Dark Blue)

**수정 내용**: 전체 바이올렛(#7c3aed) 계열 → 다크블루(#1e40af) 계열로 교체

| 변수 | 이전 (Violet) | 이후 (Dark Blue) |
|------|--------------|-----------------|
| `--color-primary` | `#7c3aed` | `#1e40af` (blue-800) |
| `--color-primary-light` | `#a78bfa` | `#60a5fa` (blue-400) |
| `--color-primary-dark` | `#5b21b6` | `#1e3a8a` (blue-900) |
| `--color-primary-hover` | `#6d28d9` | `#1d4ed8` (blue-700) |
| `--color-primary-bg` | `#f5f3ff` | `#eff6ff` (blue-50) |
| `--color-primary-surface` | `#ede9fe` | `#dbeafe` (blue-100) |
| `--color-pairwise-left` | `#7c3aed` | `#1e40af` |
| `--color-pairwise-selected` | `rgba(124,58,237,0.18)` | `rgba(30,64,175,0.18)` |
| `--color-pairwise-selected-border` | `#5b21b6` | `#1e3a8a` |
| `--color-pairwise-equal` | `#f5f3ff` | `#eff6ff` |
| `--color-priority-bar` | `#7c3aed` | `#1e40af` |
| `--color-level-1` | `#7c3aed` | `#1e40af` |
| `--color-completed` | `#7c3aed` | `#1e40af` |
| `--shadow-violet` | `rgba(124,58,237,0.15)` | `rgba(30,64,175,0.15)` |
| `--shadow-violet-lg` | `rgba(124,58,237,0.20)` | `rgba(30,64,175,0.20)` |

**하드코딩 rgba 교체**:
- `HomePage.module.css`: `rgba(124, 58, 237, ...)` → `rgba(30, 64, 175, ...)` (6곳)
- `EvaluatorMainPage.module.css`: 동일 교체 (1곳)

#### 11-2. 대시보드 폰트 크기 + 행간 확대

| 요소 | 이전 | 이후 |
|------|------|------|
| `.title` | `1.6rem` | `1.8rem` |
| `.welcome h2` | `1.3rem` | `1.5rem` |
| `.welcomeDesc` | `0.95rem` | `1.05rem` |
| `.projectTitle` | `1.35rem` | `1.5rem` |
| `.statusBadge` | `0.78rem` | `0.88rem` |
| `.projectDesc` | `0.95rem` | `1.05rem`, `line-height: 1.7` |
| `.stepRow` padding | `12px 16px` | `14px 18px` |
| `.stepLabel` | `1rem` | `1.1rem`, `line-height: 1.5` |
| `.stepDesc` | `0.85rem` | `0.95rem`, `line-height: 1.6` |
| `.stepNum` | `34px / 0.85rem` | `38px / 0.95rem` |
| `.stepList` gap | `2px` | `4px` |

#### 11-3. 연구자 가이드 패널 (AdminDashboard 오른쪽)

**새 파일**:
- `src/components/admin/ResearcherGuide.jsx` — 5개 섹션 접이식 가이드
- `src/components/admin/ResearcherGuide.module.css` — 다크블루 테마 스타일

**구성**: 프로젝트 생성 / 모델 구축 / 평가자 관리 / 결과 분석 / 논문 작성
- 개요 화면: 5개 카드 목록 (아이콘 + 제목 + 설명)
- 상세 화면: 카드별 세부 내용 + Tip 박스
- useState로 overview ↔ section-N 전환

**AdminDashboard 수정**:
- grid: `350px 1fr` → `350px 1fr 360px` (3컬럼)
- `.guidePanel` 스타일 추가 (스크롤 가능)
- 반응형: 1200px 이하에서 가이드 패널 숨김

#### 11-4. 평가자 완전 가이드 (EvaluatorMainPage 좌측 슬라이드)

**새 파일**:
- `src/components/evaluation/EvaluatorGuide.jsx` — 5개 섹션 풀 가이드
- `src/components/evaluation/EvaluatorGuide.module.css` — 슬라이드 패널 스타일

**구성**: 프로젝트 초대 / 쌍대비교 평가 / 직접입력 / 설문조사 / 완료 및 결과
- 배너 영역에 "📖 평가자 가이드" 버튼 추가
- 클릭 시 좌측에서 420px 슬라이드 패널 열림
- 프로세스 플로우 (초대 수락 → 로그인 → 평가 → 설문 → 완료)
- 개요 → 상세 전환 (← 개요로 돌아가기)
- 9점 척도 표, 통계 박스, 체크리스트, 예시 박스 등 다양한 UI 요소

| 파일 | 변경 |
|------|------|
| `src/styles/variables.css` | 전체 컬러 팔레트 교체 (바이올렛 → 다크블루) |
| `src/pages/HomePage.module.css` | rgba 하드코딩 교체 (6곳) |
| `src/pages/EvaluatorMainPage.module.css` | rgba 교체 + bannerActions 스타일 추가 |
| `src/pages/EvaluatorMainPage.jsx` | EvaluatorGuide import + 배너에 가이드 버튼 추가 |
| `src/pages/AdminDashboard.module.css` | 3컬럼 grid + 폰트 확대 + guidePanel 스타일 |
| `src/pages/AdminDashboard.jsx` | ResearcherGuide import + guidePanel 영역 추가 |
| `src/components/admin/ResearcherGuide.jsx` | 새 파일 (연구자 가이드) |
| `src/components/admin/ResearcherGuide.module.css` | 새 파일 (연구자 가이드 스타일) |
| `src/components/evaluation/EvaluatorGuide.jsx` | 새 파일 (평가자 가이드) |
| `src/components/evaluation/EvaluatorGuide.module.css` | 새 파일 (평가자 가이드 스타일) |

---

### 12. 사이드바 접기/펼치기 + 연구자 가이드 오른쪽 고정 배치

**요청**: "연구자 가이드 왼쪽메뉴처럼 오른쪽에 고정시키고, 왼쪽메뉴, 오른쪽 메뉴 모두 접었다 펴는 기능으로 화면 구성해줘"

#### 구조 변경

**이전 구조**: AdminDashboard 내부 3컬럼 그리드 (leftPanel | rightPanel | guidePanel)
**이후 구조**: ProjectLayout 레벨에서 좌/우 사이드바 고정 배치

```
ProjectLayout
├── Navbar
├── Body (flex row)
│   ├── Left Sidebar (ProjectSidebar) — 접기/펼치기 (200px ↔ 48px)
│   ├── Main Content (flex: 1)
│   │   └── AdminDashboard (2컬럼: 350px 1fr)
│   └── Right Sidebar (ResearcherGuide) — 접기/펼치기 (320px ↔ 48px)
└── Footer
```

#### 주요 변경

**ProjectLayout.jsx**:
- `useState`로 `leftOpen`/`rightOpen` 상태 관리
- ResearcherGuide를 오른쪽 고정 사이드바로 배치
- 양쪽에 원형 토글 버튼 (`‹`/`›`) 추가
- 접힌 상태에서 세로 텍스트("연구자 가이드") + 아이콘 표시

**ProjectLayout.module.css**:
- `.sidebarWrap`: 200px ↔ 48px 트랜지션 (250ms ease)
- `.sidebarWrap.right`: 320px ↔ 48px
- `.toggleBtn`: 원형 버튼 (absolute, z-index 10)
- `.collapsedGuide`: 세로 writing-mode + 아이콘
- 반응형 768px: 좌우 사이드바 세로 배치, 토글 버튼 숨김

**ProjectSidebar.jsx**:
- `collapsed` prop 수신
- 접힌 상태: 아이콘(☰ + 숫자)만 표시, 라벨 숨김
- `title` attribute로 호버 시 툴팁 제공

**ProjectSidebar.module.css**:
- `.sidebar.collapsed`: width 48px, min-width 48px
- `.collapsed .dashboardItem/.menuItem`: justify-content center

**AdminDashboard**:
- ResearcherGuide import 제거 (ProjectLayout으로 이동)
- 그리드: `350px 1fr 360px` → `350px 1fr` (2컬럼 복원)
- guidePanel 관련 스타일 모두 제거

| 파일 | 변경 |
|------|------|
| `src/components/layout/ProjectLayout.jsx` | 좌/우 사이드바 + 토글 상태 관리 |
| `src/components/layout/ProjectLayout.module.css` | 사이드바 래퍼 + 토글 버튼 + 접힘 스타일 |
| `src/components/layout/ProjectSidebar.jsx` | collapsed prop 지원 |
| `src/components/layout/ProjectSidebar.module.css` | 접힌 상태 스타일 |
| `src/pages/AdminDashboard.jsx` | ResearcherGuide 제거, 2컬럼 복원 |
| `src/pages/AdminDashboard.module.css` | guidePanel 제거, 2컬럼 복원 |

---

## 빌드 확인

- 커밋 1: `npx vite build` — 성공 (8.61s), JS 1,127KB, CSS 56.7KB
- 커밋 2: `npx vite build` — 성공 (9.25s), JS 1,131KB, CSS 60.3KB
- 커밋 3: `npx vite build` — 성공 (7.24s), JS 1,132KB, CSS 61.5KB
- 커밋 4~7: GitHub Actions 배포 — 모두 성공
- 커밋 8: `npx vite build` — 성공 (7.19s), JS 1,149KB, CSS 80.6KB
- 커밋 9: `npx vite build` — 성공 (7.06s), JS 1,151KB, CSS 82.7KB

## 점검 체크리스트

- [x] 컬러 시스템: `#7c3aed` → `#1e40af` 전환 완료
- [x] 대시보드 폰트: 11개 요소 크기/행간 확대 완료
- [x] 연구자 가이드: ProjectLayout 오른쪽 고정 사이드바 (320px ↔ 48px)
- [x] 평가자 가이드: EvaluatorMainPage 배너 슬라이드 패널
- [x] 왼쪽 사이드바: 접기/펼치기 토글 (200px ↔ 48px)
- [x] 오른쪽 사이드바: 접기/펼치기 토글 (320px ↔ 48px)
- [x] 접힌 상태: 아이콘만 표시 + 세로 텍스트
- [x] 반응형: 768px 이하 세로 배치, 토글 숨김
- [x] 빌드 성공 확인
